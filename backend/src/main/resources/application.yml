spring:
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
  datasource:
    url: jdbc:mysql://switchback.proxy.rlwy.net:21935/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: ExzjvEDybIVuEIjFKqRvDbMxxqzLesWD
  sql:
    init:
      mode: always
      platform: mysql
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false
  data:
    redis:
      host: turntable.proxy.rlwy.net
      port: 26522
      password: GAPhXUkOuxPLfsAlgLTcorlDRxcFmMtD

oauth2:
  google:
    client-id: ${GOOGLE_CLIENT_ID:}
    client-secret: ${GOOGLE_CLIENT_SECRET:}

backend:
  url: ${BACKEND_URL:http://localhost:8080}

frontend:
  url: ${FRONTEND_URL:http://localhost:5173}

jwt:
  secret: buffett-diary-jwt-secret-key-for-development-only-change-in-production-2024
  access-expiration: 1800000   # 30 minutes
  refresh-expiration: 604800000 # 7 days

server:
  port: 8080

logging:
  level:
    com.buffettdiary: DEBUG
    org.springframework.web: DEBUG
